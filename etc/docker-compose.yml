version: "3"
services:
    db:
        image: postgres
        restart: always
        environment:
            POSTGRES_USER: root
            POSTGRES_PASSWORD: q1w2e3r4
        ports:
            - 5432:5432

    adminer:
        image: adminer
        restart: always
        ports:
            - 9090:8080
